<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Modifier l'utilisateur</title>

</head>
<body>
  <h1>Modifier l'utilisateur</h1>

  <form action="{{ route('users.update', { id: user.id }) }}" method="POST">
    {{ csrfField() }}

    <div class="form-group">
      <label for="nom">Nom :</label>
      <input
        type="text"
        name="nom"
        id="nom"
        value="{{ user.nom }}"
        required
      >
    </div>

    <div class="form-group">
      <label for="email">Email :</label>
      <input
        type="email"
        name="email"
        id="email"
        value="{{ user.email }}"
        required
      >
    </div>

    <div class="form-group">
      <label for="password">Nouveau mot de passe :</label>
      <input
        type="password"
        name="password"
        id="password"
        placeholder="Laisser vide si inchangé"
        minlength="6"
      >
      <small>Laissez ce champ vide pour conserver le mot de passe actuel. Si vous le modifiez, il doit contenir au moins 6 caractères.</small>
    </div>

    <button type="submit">Mettre à jour</button>
  </form>

  <a href="{{ route('users.index') }}" class="back-link">← Retour à la liste</a>
</body>
</html>
